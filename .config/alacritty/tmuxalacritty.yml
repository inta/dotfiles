window:
  title: Tmuxalacritty
  dimensions:
    columns: 150
    lines: 50

shell:
  program: /usr/bin/fish
  args:
    - -l
    - -c
    - "tmux attach || tmux-sessionizr"

key_bindings:
  - { key: Q, mods: Control, action: Quit }
