window:
  dimensions:
    columns: 150
    lines: 50

shell:
  program: /usr/bin/fish
  args:
    - -l
    - -c
    - "zellij attach || zellij-sessionizr"

key_bindings:
  - { key: Q, mods: Control, action: Quit }
